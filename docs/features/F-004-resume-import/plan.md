# F-004 — Implementation Plan

*Status: approved · Approved by: Ryan · Date: 2026-07-03*

## Approach
Client-side extraction (`pdfjs-dist` for PDF text layer, `mammoth` for DOCX → plain text), both dynamically `import()`-ed only when the user picks a file so the main bundle is untouched for non-importing users. Extracted text is assembled into a single structured-parse prompt sent to Gemini via a **new** `importResume()` function in `src/lib/gemini.ts` that uses the `x-goog-api-key` header (per CLAUDE.md hard rule and spec constraint) rather than copying the existing `?key=` call — existing calls (`improveText`, `generateSummary`, `tailorResume`, `testConnection`) are left untouched since migrating them is out of scope and non-trivial (touches every existing caller/test). The response is validated through F-002's `parseResumeData()` (strict schema, repairs missing/blank ids, rejects wrong-typed fields) so imported data can never bypass the same guarantees JSON import already has. A new `ImportDialog` modal (mirroring `SettingsDialog`'s fixed-overlay/`print:hidden`/backdrop-click pattern) drives the whole flow — pick file → extracting → preview-before-apply (section-level summary, replaces `Toolbar`'s bare `setResume()` call) → apply or cancel — with a graceful-failure state that shows the raw extracted text for copy/paste instead of a bare error. No merge-into-existing-resume logic (spec: replace only, out of scope) and no OCR/image-PDF handling (out of scope — a text-layer-empty PDF is treated as an extraction failure, not silently guessed at).

Rejected alternative: reusing `AiPanel`'s `ApplyCard` component directly — its "apply this single field/list" per-field diff model fits AI *tailoring* increments, not a one-shot whole-resume replace with a single confirm/cancel gate (AC3). The plan reuses the *concept* (inline confirm-before-mutate card) but builds a purpose-fit preview summary instead of forcing the import flow through per-field ApplyCards.

Rejected alternative: parsing PDF/DOCX to structured data without an LLM (regex/heuristic section detection) — real-world resumes have too much layout variance (single vs. two-column, varying section headers, inconsistent date formats) for a heuristic parser to hit AC1/AC2 reliably; the spec already mandates the Gemini structured-parse step.

## Files to change
| File | Change |
|------|--------|
| `source_code/package.json` | new — add `pdfjs-dist` and `mammoth` dependencies |
| `source_code/src/lib/extract/pdf.ts` | new — `extractPdfText(file: File): Promise<string>`, dynamic-imports `pdfjs-dist`, configures worker, joins page text |
| `source_code/src/lib/extract/docx.ts` | new — `extractDocxText(file: File): Promise<string>`, dynamic-imports `mammoth`, returns raw text (`extractRawText`) |
| `source_code/src/lib/extract/index.ts` | new — `extractText(file: File): Promise<string>`, dispatches by MIME/extension to `pdf.ts`/`docx.ts`, throws a typed error for unsupported types or empty/whitespace-only extraction (image-only PDF case) |
| `source_code/src/lib/extract/index.test.ts` | new — dispatch logic, unsupported-type rejection, empty-text rejection (mocking the two extractor modules) |
| `source_code/src/lib/extract/pdf.test.ts` | new — real PDF fixture parsed end-to-end (per spec's fixture requirement) |
| `source_code/src/lib/extract/docx.test.ts` | new — real DOCX fixture parsed end-to-end |
| `source_code/src/lib/extract/fixtures/sample-resume.pdf` | new — real single-column text PDF fixture (checked in) |
| `source_code/src/lib/extract/fixtures/sample-resume.docx` | new — real DOCX of an equivalent resume (checked in) |
| `source_code/src/lib/gemini.ts` | add `importResume(extractedText: string): Promise<ResumeData>` — builds prompt, calls a new header-auth request path, `parseJson` + `parseResumeData` |
| `source_code/src/lib/gemini.test.ts` | add tests for `importResume`'s prompt assembly and response validation/repair path (Gemini call mocked) |
| `source_code/src/lib/import.ts` | new — orchestrates `extractText` → `importResume`, typed result/error union so the UI can distinguish "extraction failed" vs "AI parse failed" (both need the raw-text fallback per AC5) |
| `source_code/src/lib/import.test.ts` | new — orchestration unit tests: success path, extraction failure, AI failure (mocked), cancel path leaves store untouched |
| `source_code/src/components/ImportDialog.tsx` | new — modal: file picker → loading → preview-before-apply summary (AC3) → apply/cancel; failure state showing raw text (AC5); "no key" gating routes to Settings (AC6); privacy note; `print:hidden` |
| `source_code/src/components/ImportDialog.test.tsx` | new — UI-level tests: preview rendering, cancel leaves `useResume` untouched, no-key routes to `openSettings`, failure state shows raw text |
| `source_code/src/components/Toolbar.tsx` | keep existing JSON import as-is; add a new "Import resume" entry point (button) that opens `ImportDialog` for PDF/DOCX |
| `source_code/src/App.tsx` | mount `<ImportDialog />` alongside `<SettingsDialog />` |
| `source_code/vite.config.ts` | if needed, configure `pdfjs-dist` worker asset handling (verify default Vite `new URL(..., import.meta.url)` pattern works first; only add config if it doesn't) |

## Steps
Ordered, each independently verifiable. Check off during build.
1. [x] Install `pdfjs-dist` and `mammoth`; confirm `npm run build` still succeeds with zero usage (baseline bundle size noted for comparison in Step 16).
2. [x] Source or construct two small real fixture files — `sample-resume.pdf` (single-column text PDF: name, headline, contact, 2 experience entries with bullets, education, skills) and `sample-resume.docx` (same content) — under `src/lib/extract/fixtures/`. Keep them small and text-only (no images) so CI stays fast and deterministic.
3. [x] Build `src/lib/extract/pdf.ts`: dynamic `import('pdfjs-dist/legacy/build/pdf.mjs')` with `disableWorker: true` (see Deviations — no Worker/Vite worker-bundling needed), extract and join per-page text content. Throws on zero-length/whitespace-only output (signals an image-only/scanned PDF, which is out of scope per spec).
4. [x] Write `src/lib/extract/pdf.test.ts` against the real fixture from Step 2 — asserts extracted text contains expected name/section keywords. This is the CI fixture requirement from the spec's test-expectations section.
5. [x] Build `src/lib/extract/docx.ts`: dynamic `import('mammoth')`, `extractRawText({ arrayBuffer, buffer })` (see Deviations — passes both fields so it works regardless of which mammoth build — Node or browser — gets resolved). Throws on empty output.
6. [x] Write `src/lib/extract/docx.test.ts` against the real fixture from Step 2.
7. [x] Build `src/lib/extract/index.ts`: `extractText(file)` dispatching on `file.type`/extension to Step 3/5's functions; throws a clear "Unsupported file type" error for anything else. Write `src/lib/extract/index.test.ts` (mocks the two extractor modules to test dispatch/error paths in isolation from real parsing).
8. [x] Add `importResume(extractedText)` to `src/lib/gemini.ts`: assemble a prompt instructing Gemini to return `ResumeData`-shaped JSON (reuse the same "never invent facts" pattern as `tailorResume`, adapted to extraction — only structure what's present in the text, use empty string/array for anything not found, generate no `id` fields since `parseResumeData` will assign them). Call Gemini via a new internal request path using the `x-goog-api-key` header (not the existing `?key=` `generateContent()` — a small header-auth variant of the fetch call, local to this function; do not alter `generateContent()`). Parse with `parseJson`, then validate/repair with F-002's `parseResumeData()` (satisfies AC4 — valid ids, schema-passing data). Let `parseResumeData`'s errors and any Gemini errors propagate to the caller (`import.ts`) rather than swallowing them, so the orchestration layer can decide the failure-path UI.
9. [x] Write `src/lib/gemini.test.ts` additions: prompt assembly (given sample extracted text, prompt contains that text and the expected instructions), response validation/repair (mocked Gemini response missing ids gets repaired by `parseResumeData`; mocked malformed response throws a useful error), and confirm the new call path sends `x-goog-api-key` as a header and not a query param (inspect the mocked `fetch` call args).
10. [x] Build `src/lib/import.ts`: `importResumeFile(file: File): Promise<ResumeData>` composing `extractText` → `importResume`, with errors tagged/wrapped so the UI can show "extraction failed" vs "AI parsing failed" while always retaining the raw extracted text (when extraction succeeded but AI parsing failed) for the AC5 fallback. No calls into `useResume` here — this module returns data only, never mutates the store (keeps cancel-leaves-state-untouched trivially true).
11. [x] Write `src/lib/import.test.ts`: success path (mocked extract + mocked `importResume` → returns valid `ResumeData`), extraction-failure path (raw text unavailable, clear error), AI-failure path (raw text available and returned alongside the error for the UI to display), and confirm no `useResume` mutation happens anywhere in this module.
12. [x] Build `src/components/ImportDialog.tsx`: modal following `SettingsDialog`'s structure (`fixed inset-0 z-50 ... print:hidden`, backdrop click + `stopPropagation` on panel). States: idle/file-picker → extracting/parsing (loading) → preview (section-level summary: counts of experience/education/projects/skills/etc. parsed, plus name/headline) with "Replace current resume?" confirm + cancel → applied (closes dialog, calls `useResume.getState().setResume()` only on explicit confirm) → failure (shows error message + raw extracted text in a copyable read-only text area, matching AC5). Gate on `hasKey`/`openSettings()` exactly like `AiPanel.analyze()` (AC6). Include the privacy statement ("Your file is sent only to Google's Gemini API from your browser — never to any other server") near the file picker, matching `SettingsDialog`'s existing privacy note tone.
13. [x] Write `src/components/ImportDialog.test.tsx` (`.tsx`, not `.ts` — needs JSX, matching `Preview.test.tsx`'s precedent): preview summary renders parsed counts before apply; cancel calls neither `setResume` nor mutates `useResume` state; confirm calls `setResume` exactly once with the parsed data; no-key state routes to `openSettings` and never calls extract/import; failure state renders the raw text.
14. [x] Wire `ImportDialog` into `src/App.tsx` (mount alongside `SettingsDialog`) and add its trigger button to `src/components/Toolbar.tsx`, distinct from the existing JSON "Import" button (exact labels/icons decided at build time, not a plan-level concern). Existing JSON import path (`importResumeJson`, `src/lib/file.ts`) is untouched.
15. [ ] Manual: exercise the full flow in the browser preview — upload the fixture PDF, confirm preview summary, apply, confirm resume replaced; repeat for DOCX; repeat cancel (state untouched); repeat with no API key configured (routes to Settings); force an AI failure (e.g. temporarily break the key) and confirm raw text is shown.
16. [ ] Run `npm run lint && npm run build && npm test` — all green. Compare bundle output against Step 1's baseline to confirm `pdfjs-dist`/`mammoth` are not present in the main/eagerly-loaded chunk (only in a dynamically-imported chunk).

## Risk register
| Risk / landmine | Mitigation |
|-----------------|------------|
| `pdfjs-dist` worker file not resolving correctly under Vite (common pain point — wrong worker URL causes a silent runtime failure or CORS/MIME error) | Verify with the real PDF fixture in an actual dev-server run (not just jsdom) before considering Step 3 done; jsdom cannot run real Web Workers, so this is a manual-verification gap, not just a unit-test one |
| Gemini hallucination/misparse on unusual resume layouts (two-column, tables, non-standard section headers) not covered by the single fixture | Out of scope per spec (single-column text PDF is the AC1 bar); prompt includes explicit "never invent facts, only structure what's present" instructions matching existing `tailorResume`/`improveText` guardrails; AC3's preview-before-apply is the user-facing safety net for any misparse, not silent trust |
| Bundle size growth from `pdfjs-dist` (historically large) even with lazy-loading, if the dynamic `import()` boundary is drawn incorrectly (e.g. importing types eagerly pulls the runtime in) | Use `import type` for any type-only pdfjs/mammoth references at the top of files; runtime code only inside the dynamic `import()`; verified by bundle inspection in Step 16, not assumed |
| DOCX table/formatting edge cases (`mammoth`'s plain-text extraction flattens tables, may lose column structure resumes sometimes use for skills) | Accepted risk — spec only requires AC2 parity with the *same* resume as the PDF fixture, not arbitrary DOCX layouts; flatten-to-text is mammoth's designed behavior and matches the "extract text → let Gemini structure it" approach used for PDF too |
| New `x-goog-api-key` header call path duplicating request/error-handling logic already in `generateContent()`, risking drift between the two auth patterns | Keep the new path as a minimal, clearly-commented variant colocated in `gemini.ts` next to `generateContent`, reusing the same error-mapping logic (400/403/429 messages) rather than reimplementing it; note in a code comment that this is the intended pattern for all *future* new AI calls per CLAUDE.md, and that migrating existing calls is a follow-up, not part of this feature |
| Import applies a partial/corrupt resume if `setResume` is called before validation completes, violating AC5 ("no partial resume state is written") | `import.ts` never touches `useResume`; the dialog only calls `setResume()` once, synchronously, after the user's explicit "Replace" confirm on a fully-validated `ResumeData` object — there is no code path that writes to the store before that single call |
| Large PDF/DOCX files causing slow client-side extraction or a frozen UI | Existing loading-state pattern (spinner + disabled controls, as in `AiPanel`/`SettingsDialog`) covers UX; explicit file-size ceiling is not in the AC list, treated as out of scope but the loading state avoids the UI appearing hung |
| Privacy promise omission — user may not realize the file content leaves the browser to Google | Explicit privacy statement in `ImportDialog`, phrased consistently with the existing `SettingsDialog` API-key privacy note, satisfying the spec's constraint directly |
| Test flakiness from real PDF/DOCX fixture parsing in CI (binary fixtures, environment-dependent parsing) | Keep fixtures minimal/deterministic (plain text, no fonts/images); pin `pdfjs-dist`/`mammoth` versions in `package.json` matching the repo's existing `^` convention so CI parsing behavior doesn't drift silently |

## Test plan
- Unit:
  - `src/lib/extract/index.test.ts` — file-type dispatch, unsupported-type rejection, empty-extraction rejection (extractors mocked).
  - `src/lib/extract/pdf.test.ts` — real PDF fixture parsed to expected text (AC1 extraction layer, per spec's fixture requirement; Gemini not involved).
  - `src/lib/extract/docx.test.ts` — real DOCX fixture parsed to expected text (AC2 extraction layer).
  - `src/lib/gemini.test.ts` (additions) — `importResume` prompt assembly contains extracted text + no-invention instructions; response validation via `parseJson` + `parseResumeData` repairs missing ids and rejects malformed shapes; confirms `x-goog-api-key` header (not `?key=` query param) is used for this call specifically (Gemini fetch mocked).
  - `src/lib/import.test.ts` — orchestration success path; extraction-failure path (raw text unavailable); AI-failure path (raw text available, surfaced for fallback display); confirms zero `useResume` mutation from this module (covers "cancel/failure paths" and "no partial resume state" from AC5).
- Integration/UI:
  - `src/components/ImportDialog.test.ts` — preview-before-apply summary renders parsed section counts before any store mutation (AC3); cancel leaves `useResume` state byte-identical to before the dialog opened (AC3); confirm applies via a single `setResume` call (AC4 — validated data only); no-key state routes to `openSettings` without attempting extraction (AC6); failure state renders the raw extracted text for copy/paste (AC5); dialog root carries `print:hidden`.
- Manual verification script (for the review stage):
  1. With a Gemini key configured, import the real PDF fixture via the new dialog — confirm preview shows correct basics/experience/education/skills counts, confirm apply replaces the resume correctly (AC1).
  2. Repeat with the DOCX fixture — confirm an equivalent result to the PDF import (AC2).
  3. Open the dialog, reach the preview screen, click cancel — confirm the resume in the editor/preview pane is completely unchanged (AC3).
  4. Inspect a successfully imported resume's experience/education/etc. items — confirm every item has a non-empty `id` and the app's normal add/edit/remove/reorder interactions work on them (AC4).
  5. Force a failure (e.g. temporarily invalid API key, or a corrupt/empty file) — confirm a useful error message is shown alongside the raw extracted text (when extraction itself succeeded), and confirm the resume state was not altered (AC5).
  6. Clear the API key in Settings, then try to import — confirm it routes to the Settings dialog instead of attempting the call (AC6).
  7. Real dev-server run (not jsdom) confirming the `pdfjs-dist` worker loads correctly and PDF extraction works outside the test environment.
  8. Bundle-size check: `npm run build`, inspect output chunks, confirm `pdfjs-dist`/`mammoth` are not in the eagerly-loaded main bundle.

## Deviations
- **No PDF Worker configured — relies on `pdfjs-dist`'s built-in fake-worker fallback instead of the planned `new URL(...)` worker-bundling approach.** The plan's Step 3 anticipated needing to resolve `pdfjs-dist`'s worker file under Vite as the primary risk. Testing showed the main `pdfjs-dist` browser build requires `DOMMatrix`, which jsdom (the test environment) doesn't provide — the library itself warns "Please use the `legacy` build in Node.js environments." Switched to `pdfjs-dist/legacy/build/pdf.mjs`, which works without `DOMMatrix`. There is no `disableWorker` option in this version (an earlier pass tried one and `tsc` correctly rejected it — reverted); instead, `GlobalWorkerOptions.workerSrc` is simply left unconfigured. Reading `pdfjs-dist`'s own source (`PDFWorker#initialize` in `legacy/build/pdf.mjs`) confirms `new Worker(workerSrc, ...)` is wrapped in a try/catch that falls back to `#setupFakeWorker()` (main-thread, `LoopbackPort`) whenever real worker creation fails for any reason — including an unset `workerSrc`, which throws inside that same try block. So no special-casing is needed: Node/jsdom (no `Worker` global) and a real browser with no `workerSrc` configured both land on the same main-thread fallback path automatically, at the cost of a harmless "Setting up fake worker" console warning. Resume PDFs are small (a handful of pages of text), so main-thread parsing has no meaningful UX cost, and this sidesteps the single largest risk in the risk register (worker asset resolution under Vite) — one code path, verified identically in the test environment and the real browser preview (Step 15). Confirmed working end-to-end against the real PDF fixture in `pdf.test.ts`.
- **Added `tsconfig.vitest.json`, a third TS project referenced from the root `tsconfig.json` (not in the original plan).** `tsc -b` (part of `npm run build`) type-checks test files too, and the new fixture-reading tests need `node:fs`, `node:path`, and `__dirname` — unavailable under `tsconfig.app.json`'s browser-only `lib`/`types` (correctly so, for actual app code). Rather than polluting the whole app with Node ambient types, added `tsconfig.vitest.json` (extends `tsconfig.app.json`, adds `"node"` to `types`, scoped via `include` to `src/**/*.test.ts(x)` only) and excluded test files from `tsconfig.app.json` itself so each file is type-checked exactly once, under the config matching its actual runtime environment.
- **Added `withDefaults()` in `gemini.ts` (not in the original plan).** `parseResumeData`'s strict schema requires every string field present on every item — correct for validating a user's own JSON export, but Gemini's response won't reliably include every key for every item (e.g. a remote job with no `location`). A first pass at `importResume` without this hit `SchemaError: Invalid "experience": expected an array` on a response that only omitted an optional-seeming field. `withDefaults()` recursively fills genuinely *missing* keys from the same schema-template object already used to build the prompt (single source of truth, no drift), but leaves present-but-wrong-typed values untouched so a real malformed response (e.g. `skills` returned as a string) still fails validation and surfaces as an AI-parsing error rather than being silently coerced away. Covered by `gemini.test.ts`'s "repairs a response missing item ids" and "throws a specific error when the response has a wrong-typed field" cases.
- **`mammoth.extractRawText` called with both `arrayBuffer` and `buffer` fields.** mammoth ships two build variants selected via package.json's `browser` field remap (`lib/unzip.js` reads `options.buffer`, a Node `Buffer`; `browser/unzip.js` reads `options.arrayBuffer`). Vite's client build resolves the browser variant; Vitest (running in a plain Node process) resolves the Node variant. Passing both fields (`{ arrayBuffer, buffer: Buffer.from(arrayBuffer) }` when the `Buffer` global exists, else just `{ arrayBuffer }`) makes `extractDocxText` work correctly under whichever variant gets resolved, without needing custom Vite/Vitest resolution config. Confirmed working against the real DOCX fixture in `docx.test.ts`.
